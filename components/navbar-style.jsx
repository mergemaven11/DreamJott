import styled from 'styled-components';

const Wrapper = styled.nav`
  max-width: 1000px;
  margin: 0 auto;
  height: $nav-height;
  background: #262626;
  height: 70;
}
`;
export default {
  Wrapper,
};
